source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '2.6.5'

instance_eval File.read('Gemfile.essential')

gem 'bcrypt', '3.1.12' # to avoid error on devise
gem 'devise' # user session
gem 'pagy' # pagination
gem 'slim-rails'

#gem 'redcarpet' # markdown
#gem 'metainspector' # extra meta from uri
#gem 'twitter-text' # extra uri from message
#gem 'closure_tree' # note hierarchy
#gem 'line-bot-api' # LINE messenger
#gem 'telegram-bot' # telegram
#gem 'sendgrid-actionmailer' # SendGrid
#gem 'montrose' # date recurrence

gem 'redis'
gem 'hiredis'
